<%- include("_header")-%>

<main class="main-content"> <!-- Envolve todo o conteúdo principal -->

    <!-- Seção: Sobre a ONG -->
    <section id="sobre" class="section-sobre"> <!-- Usando section e ID -->
        <h1>O que é a ONG Amor Animal?</h1>
        <p>A ONG Amor Animal é uma organização sem fins lucrativos dedicada à proteção e bem-estar dos animais. Nossa missão é resgatar, cuidar e encontrar lares amorosos para animais abandonados, promovendo a adoção responsável e a conscientização sobre a importância do respeito aos direitos dos animais.</p>
        <p>Estamos comprometidos em fazer a diferença na vida dos animais e na sociedade, trabalhando em parceria com a comunidade para promover um mundo mais justo e solidário.</p>
        <p>Você pode fazer a diferença na vida de um animal abandonado. Adote, doe ou faça parte da nossa equipe de voluntários!</p>
        <p>Juntos, podemos transformar vidas e construir um futuro melhor para os animais!</p>
        <p>Se você deseja fazer parte dessa causa, entre em contato conosco e descubra como pode ajudar!</p> 
    </section>

    <!-- Seção: Últimas Notícias -->
    <section id="noticias" class="section-noticias">
        <h2>Últimas Notícias</h2> <!-- Usando H2 para título da seção -->
        <p>Fique por dentro das últimas novidades e eventos da ONG Amor Animal. Aqui você encontrará informações atualizadas sobre nossas atividades, campanhas de adoção, eventos beneficentes e muito mais!</p>
        <p>Acompanhe nossas redes sociais e fique por dentro de tudo o que acontece na nossa ONG!</p>

        <div class="noticias-lista"> <!-- Container para os itens de notícia -->
            <% if (typeof model1 !== 'undefined' && model1.length > 0) { %>
            <% for(const home of model1) { %>
            <div class="noticia-item"> <!-- Classe específica para cada item -->
                <form action="/delete_home/<%=home.id%>/<%=home.arquivo%>" method="post" class="form-delete">
                    <input type="submit" value="X" class="btn btn-delete" onclick="return confirm('Deseja realmente excluir?')">
                </form>
                <p class="noticia-data"><%= home.data %></p>
                <img class="noticia-img img-thumbnail" src="/static/uploads/home/<%= home.arquivo %>" alt="Imagem da notícia: <%= home.titulo %>" /> <!-- Caminho raiz e classe para imagem -->

                <% if (home?.titulo && home?.mensagem) { %> <!-- Verifica ambos -->
                <h3 class="noticia-titulo"><%= home.titulo %></h3> <!-- H3 para título da notícia -->
                <p class="noticia-mensagem"><%= home.mensagem %></p>
                <% } %>
            </div>
                <% } %>
                <% } else { %>
                <p>Nenhuma notícia disponível.</p>
            <% } %>
        </div>
         <a href="form_home" class="link-novo"> <!-- Link para adicionar novo item -->
            <p>Novo &gt;</p>
        </a>
    </section>

    <!-- Seção: Adoção -->
    <section id="adocao" class="section-adocao">
        <h2>Adoção</h2>
        <h3>Adote um PET e faça a diferença na vida dele!</h3> <!-- H3 como subtítulo -->
        <p>Adotar um animal é um ato de amor e responsabilidade. Ao adotar, você não só ganha um novo amigo, mas também ajuda a salvar uma vida.</p>
        <p>Na ONG Amor Animal, temos muitos pets esperando por um lar amoroso. Cada um deles tem uma história única e está pronto para dar e receber amor.</p>

        <!-- Photobanner para pets -->
        <div class="container photobanner-container"> <!-- Container para o banner -->
            <div class="photobanner" id="slides-adocao"> <!-- ID específico -->
                <% if (typeof model2 !== 'undefined' && model2.length > 0) { %>
                <% for(const adocao of model2) { %>
                <a href="/adote" class="adocao-card"> <!-- Link/Card do pet -->
                    <img src="/static/uploads/adocao/<%= adocao.arquivo || 'default.jpg' %>" alt="Foto de <%= adocao.nome %> para adoção" class="adocao-img img-medium" /> <!-- Classe para imagem -->
                    <p class="adocao-nome"><%= adocao.nome %></p>
                </a>
                <% } %>
                <% } else { %>
                <a href="/adote" class="link-adicionar-pet">
                        <p>Adicionar um Pet.</p>
                    </a>
                <% } %>
            </div>
        </div>

        <!-- Contagem de Adotantes -->
        <div class="contagem-adotantes">
            <% if (typeof model3 !== 'undefined' && model3.length > 0 && model3[0]) { %>
            <% const contagemAdotantes = Object.values(model3[0])[0] || 0 %>
                <p>Adotantes: <span class="numero-destaque"><%= contagemAdotantes %></span> pessoas.</p>
            <% } else { %>
                <p>Adotantes: Nenhum dado disponível</p>
            <% } %>
             <a href="./adotante" class="link-veja-mais">Ver Adotantes &gt;</a>
        </div>

        <!-- Histórias de Adotados -->
        <div class="historias-adotados">
            <h3>Histórias de Adotados</h3> <!-- H3 -->
            <p>Você pode contar a história do seu pet adotado e inspirar outras pessoas a também adotarem.</p>

            <% if (typeof model4 !== 'undefined' && model4.length > 0) { %>
            <% for(const adotado of model4) { %>
            <div class="historia-item">
                <img src="/static/uploads/adotado/<%= adotado.arquivo %>" alt="Foto de <%= adotado.pet %>, adotado por <%= adotado.tutor %>" class="historia-img img-medium"/>
                <h4 class="historia-tutor"><%= adotado.tutor %></h4> <!-- H4 -->
                <p class="historia-texto"><%= adotado.historia %></p>
                <h4 class="historia-pet"><%= adotado.pet %></h4> <!-- H4 -->
                <p class="historia-origem">Origem: <%= adotado.origem %></p>
            </div>
                <% } %>
            <% } else { %>
            <a href="/form_adotado" class="link-adicionar-historia">
                    <p>Adicione sua história.</p>
                </a>
            <% } %>
        </div>
         <a href="./adocao" class="link-veja-mais">
            <p>Veja Mais sobre Adoção &gt;</p>
        </a>
    </section>

    <!-- Seção: A ONG em Números (Castração) -->
    <section id="numeros" class="section-numeros">
        <h2>A ONG em Números</h2>

        <div class="numeros-castracao">
            <h3>Castração</h3>
            <!-- Contagem de Castração -->
            <% if (typeof model5 !== 'undefined' && model5.length > 0 && model5[0]) { %>
            <% const contagemCastracao = Object.values(model5[0])[0] || 0 %>
            <p class="contagem-castracao">Castrações realizadas: <span class="numero-destaque-alt"><%= contagemCastracao %></span></p>
            <% } else { %>
                <p class="contagem-castracao">Castrações realizadas: Nenhum dado disponível</p>
            <% } %>

            <!-- Tabela de Castrações Recentes -->
            <% if (typeof model6 !== 'undefined' && model6.length > 0) { %>
                <div class="tabela-container"> <!-- Container para rolagem se necessário -->
                    <table id="data-castracao" class="tabela-dados"> <!-- ID e classe específicos -->
                        <thead> <!-- Usar thead para cabeçalhos -->
                            <tr>
                                <th>Ticket</th>
                                <th>Dia</th>
                                <th>Clínica</th>
                                <th>Tutor</th>
                            </tr>
                        </thead>
                        <tbody class="tabela-corpo"> <!-- Usar tbody para corpo da tabela -->
                        <% for(const sql_castracao of model6) { %>
                        <tr>
                            <td class="coluna-ticket"><%= sql_castracao.ticket %></td>
                            <td><%= sql_castracao.origem %></td>
                            <td><%= sql_castracao.clinica %></td>
                            <td class="coluna-nome"><%= sql_castracao.nome %></td>
                        </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <p>Nenhum registro de castração recente para exibir.</p>
                <% // Não precisa mais do script para esconder a tabela se ela não for renderizada %>
            <% } %>
             <a href="./castracao" class="link-veja-mais">
                <p>Veja Mais sobre Castração &gt;</p>
            </a>
        </div>
    </section>

    <!-- Seção: Procura-se -->
    <section id="procurase" class="section-procurase">
        <h2>Procura-se</h2>
        <h3>Quer ajudar a encontrar um PET desaparecido?</h3>
        <h4>ANUNCIE, nossa rede voluntária gostaria de ajudar!</h4>
        <p>Se você tem um PET desaparecido e quer ajuda para encontrá-lo, ADICIONE!</p>

        <div class="procurase-lista">
            <% if (typeof model7 !== 'undefined' && model7.length > 0) { %>
            <% for(const procura_se of model7) { %>
            <div class="procurase-item">
                <img src="/static/uploads/procura_se/<%= procura_se.arquivo %>" alt="Foto de <%= procura_se.nomePet %> desaparecido" class="procurase-img img-medium"/>
                <p class="procurase-data">Desaparecido em: <%= procura_se.data %></p>
                <p class="procurase-nome">Nome: <%= procura_se.nomePet %></p>
                <p class="procurase-caracteristicas">Características: <%= procura_se.caracteristicas %></p>
                <p class="procurase-local">Visto por último em: <%= procura_se.local %></p>
                <p class="procurase-contato">Qualquer notícia, avise: <span class="contato-destaque"><%= procura_se.contato %></span></p> 
            </div>
                <% } %>
            <% } else { %>
                <p>Nenhum animal sendo procurado no momento.</p>
            <% } %>
        </div>
         <a href="./procura_se" class="link-veja-mais">
            <p>Veja Mais sobre Procura-se &gt;</p>
        </a>
    </section>

    <!-- Seção: Parcerias -->
    <section id="parcerias" class="section-parcerias">
        <h2>Parcerias</h2>
        <p>Estas são as empresas que nos apoiam:</p>
        <div class="container photobanner-container"> <!-- Reutilizando container -->
            <div class="photobanner" id="slides-parcerias"> <!-- ID específico -->
                <% if (typeof model8 !== 'undefined' && model8.length > 0) { %>

                <% for(const parceria of model8) { %>
                <div class="parceria-item"> <!-- Item individual -->
                    <img src="/static/uploads/parceria/<%= parceria.logo %>" alt="Logo <%= parceria.empresa %>" class="parceria-logo"/>
                    <h3 class="parceria-empresa"><%= parceria.empresa %></h3>
                    <p class="parceria-proposta"><%= parceria.proposta %></p>
                </div>
                <% } %>
                <% } else { %>
                    <p>Nenhuma parceria registrada no momento.</p>
                <% } %>
            </div>
        </div>
        <a href="./parceria" class="link-veja-mais">
            <p>Veja Mais sobre Parcerias &gt;</p>
        </a>
    </section>

</main> <!-- Fim do main-content -->

<%- include("_footer")-%>